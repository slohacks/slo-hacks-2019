import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  headerFontFamily: [
    'Helvetica Neue',
    'Helvetica',
    'sans-serif',
  ],
  headerColor: '#465A64',
  bodyFontFamily: [
    'Helvetica Neue',
    'Helvetica',
    'sans-serif',
  ],
});

export default typography;
